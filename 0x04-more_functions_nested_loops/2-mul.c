#include "main.h"
/**
 * mul - Function multiplies two integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Return: 'x' that is the result of operation
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
